
1. (1) x is 2
   (2) x is 3
   (3) x is 1
   (4) z is 7

2. (1) y = 1
   (2) y = 1

3. 10times looping,  2,4,6,8,10
theInput = raw_input("Enter an integer: ")

4. No execution since when i =0, the while statement is not satisfied

5.
theInput = int(theInput) #convert string input to integer
if theInput%2 == 0:
      print 'even'
else:
      print 'odd'


to be continuous
while True:
    theInput = raw_input("Enter an integer: ")

    theInput = int(theInput) #convert string input to integer
    if theInput%2 == 0:
          print 'even'
    else:
          print 'odd'
